

if __name__ == '__main__'
tipos de datos
str(), int()

"""
multiples lineas
"""

metodo type
manejo de strings
upper, lower, capitalize, title, strip, replace,split
' '.join(lista)
find, index, funcionan igual pero index lanza un a excepcion cuando no encuentra el valor
startswith, endwith, isalnum, isalpha, slicing de strings var[:::] util para 
recortar strings
isnumeric, isdigit, isdecimal, 
la diferencia es el alcance isdecimal solo da true con (0-9), isdigit() da true con numeros (0-9) y sub indices
isnumeric() es mucho mas amplio pudiend aceptar hasta numeros romanos. importante que estos numeros deben validarse
como unicode, no pasar numero romano directamente.
formato de string f"luis {variable} { 'oye si' if var < 20 else 'oye no'}" *** var.format( var = 52)  ** .2f

tipos de datos numericos
no hay tipos de numericos precisos

 from decimal import getContext(), Decimal getcontext().prec = especifica la precision, cuantos decimales dejar
 from decimal import Decimal, getcontext

# Configuro la precisión en 5 dígitos
getcontext().prec = 5  

a = Decimal(1) / Decimal(7)
print(a)  # 0.14286
 operaciones aritmeticas
 a ** 2
 // division entera
 condicionales
 funcion len(caracteres o elementos)
 ciclos
 for i in range(5) range(2,6,3)
 while
 while True:
	break
	forma de usar un switch-case en python
	match number:
		case 1:
			print("aja")
		case 2:
			print("color")
		case _:
			print("opcion por defecto si ninguna de las opciones coincide")
tambien puedes usar operadores logicos en el case = case 'luis' | 'maria' :

metodo __bool__

las variables de texto y lista vacias devuelven valores booleanos falsos, los numeros igual a 0 tambien.
este comportamiento tambien puede usarse demanera explicita con los metodos __le__ en el caso de cracteres y listas
y con __bool__ en el caso denumeros. tambien  se puede usar None es muy similar a undefined, y se valida en condiconales
usando is not None, is None

** manejo de excepciones en entrada de datos **

def datos():
  nombre = input("Por favor ingresa tu nombre: ")
  edad = int(input("Por favor ingresa tu edad: "))

  print(f"Nombre: {nombre}  edad: {edad}")


try:
  datos()
except ValueError:
  print("GUEEE datos ingresados incorrectamente")
  datos()